#include <iostream>

using namespace std;

int main(void) {

    //True e flase são objetos boleanos
    bool isNumber = true; //Definindo variáveis boleanas
    bool isPositive = true, isNative = false;

    bool condition;
    condition = (isNumber && isPositive); //Operador lógico &&

    return 0;
}
